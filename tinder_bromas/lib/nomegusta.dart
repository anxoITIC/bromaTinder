// nomegusta.dart
import 'package:flutter/material.dart';

class NomegustaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('No me Gusta'),
      ),
      body: Center(
        child: Text('Esta es la página de No Me Gusta'),
      ),
    );
  }
}
